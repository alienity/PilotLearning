set(TARGET_NAME Shaders)

file(GLOB_RECURSE SHADER_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/hlsl/*.hlsl)

source_group(TREE "${CMAKE_CURRENT_SOURCE_DIR}" FILES ${SHADER_FILES})

add_library(${TARGET_NAME} INTERFACE ${SHADER_FILES})

set_target_properties(${TARGET_NAME} PROPERTIES FOLDER "Engine")
